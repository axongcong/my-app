version: '3.8'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
      ports:
      - "3000:80"
    volumes:
      - ./config/docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    environment:
      - NODE_ENV=production
